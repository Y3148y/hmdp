# Token 鉴权方案对比：有状态 Token vs JWT

> 日期：2026-07-17
> 类型：知识储备文档，不涉及代码改动。
> 来源：hm-token-demo 双 Token 模块完成后的三个延伸问题——登出即时性、黑名单、方案选型。
> 关联：`spring-security-knowledge.md`、`hm-token-demo/CHANGELOG_09_token-demo.md`

---

## 一、问题起点：登出后 accessToken 还能用吗？

**能。** 登出（`/auth/logout`）只删了 Redis 里的 refreshToken，accessToken 是无状态 JWT——服务端不存它，验签只看签名和过期时间，所以在剩余的 15 分钟内它依然有效。

| Token | 登出后状态 | 原因 |
|---|---|---|
| refreshToken | 立即失效 | Redis 里删了，`/auth/refresh` 查不到 |
| accessToken | 最多还能用 15 分钟 | JWT 无状态，服务端没地方"吊销"它 |

**登出的真实语义**：是"不能再续期"，不是"立即断开"。这是无状态 JWT 的固有代价，和"刷新后旧 accessToken 还能用"是同一个根因——服务端不存 token，就没有地方标记它作废。

### 如果要求登出即时生效：黑名单方案

1. 登出时把 accessToken 的 jti（或 token 哈希）写进 Redis，TTL 设为该 token 的剩余有效期（≤15min，不会堆积）
2. `JwtAuthenticationFilter` 验签通过后多查一次 Redis：在黑名单里 → 401

代价：每个请求多一次 Redis 查询，JWT"无状态、不查库"的优势打折。生产上的常见权衡：**accessToken 有效期足够短时，接受这个窗口，不做黑名单**；只有安全极敏感的系统（支付、后台管理）才加。

---

## 二、平常的 App 有黑名单吗？

大部分**用不到**——因为它们根本不是纯 JWT 方案。分三类：

### 1. 有状态 Token 方案（大多数国内 App：淘宝、微信、美团）

Token 本身就存在服务端（Redis/Session），每个请求都查一次才放行——就是 hm-dianping 主项目的做法：

```
登录 → Redis 存 login:token:{uuid} → 每次请求查 Redis
登出 → 直接 DEL 这个 key → 下一个请求立即 401
```

Token 在服务端手里，删了就失效，**天然即时下线，不需要"黑名单"这个概念**。黑名单是纯 JWT 才需要的补丁——正因为服务端不存 token，才要反过来存"作废名单"。

### 2. 纯 JWT / 双 Token 方案（开放平台 API、微服务内部调用）

多数**不做黑名单**，靠短有效期兜底（15min 窗口可接受）。登出只吊销 refreshToken——hm-token-demo 就是这个做法。

### 3. 高敏感系统（银行、支付、后台管理）

要么加黑名单，要么干脆用方案 1 的有状态 token。改密码、异地登录检测这类场景必须即时踢下线，15 分钟窗口不可接受。

### 三种做法对比

| 方案 | 登出即时生效？ | 代价 |
|---|---|---|
| 有状态 Token（Redis 存 token） | ✓ 天然支持 | 每请求查一次 Redis |
| 纯 JWT 无黑名单 | ✗ 等自然过期 | 无 |
| 纯 JWT + 黑名单 | ✓ | 每请求查一次 Redis——成本退化回方案 1 |

**反直觉结论**：JWT + 黑名单的运行成本 ≈ 有状态 Token，无状态优势基本清零。这就是为什么普通 App 直接选有状态方案，JWT 更适合"服务端不想存任何东西"的场景。

---

## 三、主项目的做法是"完美"的吗？

不能说完美，只能说**适合它的场景**。有状态 Token 也有代价，只是在主项目的场景里这些代价不痛。

### 有状态 Token（主项目）的代价

| 代价 | 说明 |
|---|---|
| 每个请求都查 Redis | 拦截器每次 `GET login:token:{uuid}`，Redis 成了所有请求的必经之路 |
| Redis 是单点依赖 | Redis 挂了 → 全站没人能通过鉴权（JWT 验签是纯本地计算，不依赖外部服务） |
| 跨服务要共享 Redis | 微服务下每个服务都得连同一个 Redis 才能验 token；JWT 只要各服务有密钥就能本地验签 |
| 存储随在线用户增长 | 100 万在线用户 = 100 万个 token key；JWT 服务端零存储 |

### 为什么主项目里这些代价不痛

- 单体应用，本来就重度依赖 Redis（缓存、秒杀、分布式锁），鉴权多查一次不增加新依赖
- Redis 查询 <1ms，性能影响可忽略
- 单体不存在跨服务验签问题

---

## 四、"用了 Redis 就不需要 JWT 了？"——对一半

**在单体系统里成立**：既然每个请求都要查 Redis，JWT 的无状态优势清零，直接存随机 token 更简单。但两个场景下这个结论不成立：

### 1. 双 Token 方案里，Redis 和 JWT 不是二选一，是分工

看查询频率的差距：

```
有状态方案：   每个请求都查 Redis        → QPS 10000 的系统，Redis 扛 10000 次/秒鉴权查询
双 Token 方案：只有 /auth/refresh 查 Redis → 每个用户 15 分钟才查 1 次
              99% 的请求走 JWT 本地验签   → 纯 CPU 计算，0 次网络 IO
```

JWT 的价值不是"不用 Redis"，而是**把鉴权查询从"每个请求"降到"每 15 分钟一次"**——请求量大的系统这是几个数量级的差距。

### 2. 跨服务/第三方场景，没法让对方查你的 Redis

- **微服务**：订单、商品、支付服务都要验用户身份。有状态方案要求所有服务连同一个 Redis（网络打通、密码共享、强耦合）；JWT 把密钥/公钥发给各服务，各自本地验签
- **开放平台**：第三方开发者的服务器调你的 API，不可能把你的 Redis 暴露给他们

### 澄清一个误区：查 Redis ≠ "查数据库对比"

密码比对只在**登录那一次**发生。之后每次请求查 Redis 只是 `GET token → 拿到 userId`——一次 key 查找，不碰 MySQL、不比密码。

---

## 五、选型总结（一张表记住）

| 场景 | 该用什么 | 理由 |
|---|---|---|
| 单体 + 已有 Redis | 随机 token 存 Redis（主项目做法） | 即时吊销、实现简单，JWT 在此多余 |
| 高 QPS、想减轻 Redis 压力 | JWT accessToken + Redis 只管 refreshToken | 鉴权查询降几个数量级 |
| 微服务 / 开放 API | JWT，本地验签 | 不依赖共享存储，服务间解耦 |
| 高敏感系统（支付、后台） | 有状态 token，或 JWT + 黑名单 | 必须即时踢下线 |

**核心结论**：没有完美方案，只有场景匹配。JWT 解决的是"验签不依赖中心存储"，这个能力在跨服务场景无可替代；单体系统里它确实不如 Redis 存 token 直接。

---

## 六、面试话术

**面试官问"JWT 怎么实现登出/踢人下线？"**

> JWT 无状态，服务端不存 token，所以没法直接吊销——登出的标准做法是吊销 refreshToken（Redis 删 key），accessToken 靠短有效期（15 分钟）自然过期。如果业务要求即时下线，加黑名单：登出时把 token 标识写进 Redis，TTL 设为剩余有效期，过滤器验签后多查一次黑名单。但要说清代价——加了黑名单每个请求都查 Redis，无状态优势就没了，成本等于有状态 token。所以高敏感系统往往干脆用 Redis 存 token 的有状态方案。

**面试官问"你项目里为什么用 Redis 存 token 而不是 JWT？"**

> 场景决定的。主项目是单体应用，本来就重度依赖 Redis，鉴权多查一次 Redis 不增加新依赖，还换来即时吊销能力——登出删 key 立即生效。JWT 的核心价值是本地验签不依赖中心存储，这在微服务和开放 API 场景才发挥作用；单体里每个请求反正都能摸到 Redis，JWT 的优势发挥不出来，反而失去吊销能力。我在另一个双 Token 模块里用了 JWT，那个场景是开放 API——第三方服务器调接口，不可能让他们查我的 Redis。

**面试官问"有状态 token 和 JWT 各自的优缺点？"**

> 有状态 token：优点是服务端完全掌控——即时吊销、即时踢人；缺点是每个请求查一次存储，Redis 成为单点依赖，跨服务要共享存储。JWT：优点是本地验签零查询、服务端零存储、跨服务天然解耦；缺点是签出去就管不了，吊销要靠短有效期或黑名单兜底。双 Token 是折中——accessToken 用 JWT 扛住 99% 请求的验签，refreshToken 存 Redis 保留吊销控制权，把查询频率从每个请求降到每 15 分钟一次。
