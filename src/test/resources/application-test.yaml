# ============================================
# 测试环境配置 — 与开发环境完全隔离
# 激活方式: spring.profiles.active=test
# 放在 src/test/resources 下，仅测试 classpath 可见
# ============================================
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/hmdp_test?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&characterEncoding=utf8
    username: root
    password: MySQL901
  redis:
    host: 192.168.119.128
    port: 6379
    database: 1               # 独立 Redis db，与开发 db 0 隔离
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 1
        time-between-eviction-runs: 10s
